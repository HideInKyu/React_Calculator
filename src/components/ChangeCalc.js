import React from "react";

const ChangeCalc = (props) => {
    return (
        <div className="no-select circleButton">
            <img className="ChangeCalc" src={props.img} alt="" srcset="" />
        </div>
    );
};

export default ChangeCalc;
